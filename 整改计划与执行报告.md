# Review 整改计划与执行报告

## 1. 评审来源
- `REVIEW_REPORT.md`
- `UI-REVIEW-REPORT.md`

## 2. 本轮核查结论
本轮优先处理 P0/P1 且可快速闭环的问题，主要聚焦：
- 后端流程正确性（状态机、权限冲突）
- 前端关键入口缺失
- 高风险交互方式（prompt）

## 3. 本轮整改项（已完成）

### 3.1 后端：验收状态校验缺失（已修复）
- 文件：`app/services_claims.py`
- 变更：`accept_deliverable` 增加前置校验，只有 `submitted/needs_rework` 可验收；终态不可重复验收。
- 结果：避免重复验收覆写状态及潜在重复奖励。

### 3.2 后端：验收利益冲突（已修复）
- 文件：`app/services_claims.py`
- 变更：禁止验收人验收自己的交付（`actor_id == claim.lead_user_id` 时返回 403）。
- 结果：避免“自己验收自己”冲突。

### 3.3 后端：OVERDUE 无退出路径（已修复）
- 文件：`app/services_claims.py`
- 变更：`abandon_claim` 允许 `overdue` 状态执行放弃；放弃 overdue claim 时回收 1 次 `overdue_count`。
- 结果：overdue claim 可闭环，不再死锁。

### 3.4 后端：提交者审核自己问题（已修复）
- 文件：`app/services_problems.py`
- 变更：`review_problem` / `budget_review_problem` 增加 submitter 自审拦截（403）。
- 结果：避免评审环节利益冲突。

### 3.5 前端：奖励复核页未接入路由（已修复）
- 文件：`web/src/App.tsx`
- 变更：接入 `RewardReviewPage` 路由与侧边栏入口（admin/reviewer 可见）。
- 结果：奖励确认流程具备 UI 入口。

### 3.6 前端：RewardReviewPage 文案乱码（已修复）
- 文件：`web/src/pages/RewardReviewPage.tsx`
- 变更：重写为正常中文文案，统一状态/角色显示。
- 结果：页面可读可用。

### 3.7 前端：假设验证使用 prompt（已修复）
- 文件：`web/src/pages/HypothesisVerificationPage.tsx`
- 变更：将 prompt 交互替换为页面内 Modal 表单（验证方法 + 验证结果），含 `role="dialog"` 与 `aria-modal`。
- 结果：交互一致性与可访问性提升。

## 4. 自动化验证

### 4.1 后端测试
- 命令：
  - `pytest tests/test_flow.py -k "accept_deliverable_rejects_when_already_finalized or acceptor_cannot_accept_own_deliverable or overdue_claim_can_be_abandoned_and_count_reduced or submitter_cannot_review_own_problem"`
- 结果：4 passed

### 4.2 前端构建
- 命令：`npm run build`（`web/`）
- 结果：通过

## 5. 新增回归用例（已加入）
- `test_accept_deliverable_rejects_when_already_finalized`
- `test_acceptor_cannot_accept_own_deliverable`
- `test_overdue_claim_can_be_abandoned_and_count_reduced`
- `test_submitter_cannot_review_own_problem`

## 6. 下一批建议整改（未在本轮执行）
- 揭榜并发竞态：增加数据库唯一约束或悲观锁（FOR UPDATE）
- 关键危险操作统一二次确认（删除模型、放弃揭榜、资金确认）
- 表格移动端适配与侧边导航移动端体验重构
- 全站状态徽标（Badge）统一、CSS 变量治理、可访问性焦点样式完善

## 7. 本轮补充整改（已完成）

### 7.1 P0：积分规则与分配一致性
- 文件：
  - `app/schemas.py`
  - `app/services_rewards.py`
- 变更：
  - 新增 `LEVEL_POINTS_RANGE` 并在 `TaskDefinition/PricingDefinition` 做积分范围校验与默认值回填。
  - 奖励确认前后积分分配与金额分配统一为“提交人 + 执行人池 + 团队比例二次分配”。
- 结果：
  - 积分发放逻辑与金额分配逻辑一致，避免“团队成员全额重复记分”。

### 7.2 P1：预定义徽章库与下拉选择
- 文件：
  - `app/badges.py`
  - `app/routers/system.py`
  - `web/src/pages/ReviewWorkbenchPage.tsx`
  - `web/src/pages/BudgetReviewPage.tsx`
  - `web/src/pages/PersonalCenterPage.tsx`
- 变更：
  - 增加系统徽章定义与 `/badges` 接口。
  - 评审定价页改为预定义徽章下拉选择，展示等级积分范围提示。
  - 个人中心展示徽章元数据（图标/描述/来源/获得时间）。
- 结果：
  - 徽章体系从自由文本升级为结构化管理，前后端一致。

### 7.3 P2：自动徽章授予
- 文件：
  - `app/badges.py`
  - `app/services_rewards.py`
  - `app/models.py`
- 变更：
  - 新增 `UserBadge` 模型与唯一约束。
  - 在奖励确认时自动检查并授予里程碑/协作/效率类徽章。
- 结果：
  - 徽章支持“任务授予 + 自动成就授予”双轨机制。

### 7.4 遗留整改：并发、交互与可访问性
- 文件：
  - `app/models.py`
  - `app/services_claims.py`
  - `web/src/components/StatusBadge.tsx`
  - `web/src/styles/variables.css`
  - `web/src/styles/responsive.css`
  - `web/src/styles/layout.css`
  - `web/src/pages/ExecutionLoopPage.tsx`
  - `web/src/pages/ClaimApprovalPage.tsx`
  - `web/src/pages/RewardReviewPage.tsx`
  - `web/src/pages/OperationLogsPage.tsx`
- 变更：
  - `claim(task_id, lead_user_id, status=active)` 增加部分唯一索引，落地并发兜底。
  - 放弃揭榜、奖励确认、审批操作统一确认弹窗。
  - 引入统一状态徽标组件并补充焦点样式；移动端导航与表格可读性重构。
  - 操作日志详情改为可折叠 JSON 结构展示。
- 结果：
  - 高风险交互更可控，移动端和运维可读性显著提升。

### 7.5 飞书集成页功能核查与修复
- 文件：`web/src/pages/FeishuPage.tsx`
- 变更：
  - 修复同步按钮重复点击风险（加入同步中状态与禁用态）。
  - 增加 `code` 必填校验、频率输入范围校验（5-10080）。
  - 增加部门空态提示。
- 结果：
  - 飞书页面基础操作链路更稳定，误操作率降低。

### 7.6 迁移脚本补齐
- 新增：`migration_points_badges_p0p2.py`
- 作用：
  - 补齐 `deliverable.rework_count` 字段
  - 创建 `userbadge` 表及索引
  - 创建 `uq_claim_task_lead_active` 活跃揭榜唯一索引（含重复数据检测）

## 8. 本轮验证结果

### 8.1 后端测试
- 命令：`pytest -q`
- 结果：`30 passed`

### 8.2 前端构建
- 命令：`npm run build`（`web/`）
- 结果：通过
