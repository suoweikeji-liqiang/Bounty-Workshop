# 基础履责 / 失职判断模块  
##（与责任型绩效体系 R1–R5 的系统融合需求）

---

## 1. 背景与目标

当前系统已具备以下绩效治理能力：

- 任务风险分级（T1–T5）
- 责任等级（R1–R5）
- 团队轨（Tt1–Tt4）
- 小团队适配规则
- 长期 R3（合格但停滞）提醒
- OKR → T 级任务拆分

但系统尚缺少对 **“基础履责 / 明确失职”** 的统一、可自动化判断机制，导致：

- 无 T3+ 任务时，绩效下限不清晰
- 明确失职行为与 T3+ 风险承担未形成统一裁定逻辑
- 绩效评价过度依赖人工印象

本模块目标：

1. 自动识别基础履责与失职信号  
2. 生成统一的基础履责状态  
3. 将基础履责状态与 R1–R5 最终绩效等级融合  
4. 不惩罚失败，只裁定责任违约  

---

## 2. 核心定义

### 2.1 基础履责状态（唯一输出）

```text
baseline_responsibility_status ∈ { good, normal, fault }
| 状态     | 含义          |
| ------ | ----------- |
| good   | 履责良好，无结构性问题 |
| normal | 履责一般，有可改进点  |
| fault  | 明确失职，应下调绩效  |
说明：
这是 减法系统，不是加分系统
若无任何信号触发，默认 good

3. 自动识别信号（五大类）
3.1 交付 / 质量事故信号

触发来源

线上严重事故

大规模 Bug

回滚、硬件批量返修

字段建议

incident_severity   TEXT   -- none / minor / major
incident_count      INTEGER


判定规则

任意一次 major → fault

两次及以上 minor → normal

3.2 职责内任务遗漏或延误

触发来源

明确职责的 T1/T2 任务未完成

无合理理由的延期

字段建议

missed_deadline_count        INTEGER
unjustified_delay_count     INTEGER


判定规则

关键任务遗漏且无正当理由 → fault

多次非关键延误 → normal

3.3 流程合规违规（红线）

触发来源

跳过评审

跳过测试

违规上线或发布

字段建议

process_violation_count INTEGER


判定规则

任意一次明确流程违规 → fault

说明：
此类信号为一票否决项，不可被 T3+ 成果抵消。

3.4 已知风险未上报

触发来源

已知缺陷未记录

风险事后被证明“可预见但未上报”

字段建议

known_risk_unreported BOOLEAN


判定规则

一经确认 → fault

3.5 重复性问题信号

触发来源

同类问题反复出现

无改进措施或复盘

字段建议

repeated_issue_count INTEGER


判定规则

重复出现但有改进 → normal

跨周期重复且无改进 → fault

4. 基础履责状态计算规则
4.1 优先级规则（写死）
若触发任一 fault 条件 → baseline = fault
否则若触发 ≥1 normal 条件 → baseline = normal
否则 → baseline = good

4.2 伪代码示例
function calcBaselineStatus(signals) {
  if (
    signals.hasMajorIncident ||
    signals.processViolationCount > 0 ||
    signals.knownRiskUnreported
  ) {
    return 'fault'
  }

  if (
    signals.minorIncidentCount >= 2 ||
    signals.unjustifiedDelayCount > 0 ||
    signals.repeatedIssueCount > 0
  ) {
    return 'normal'
  }

  return 'good'
}

5. 与责任绩效等级（R1–R5）的融合规则
5.1 融合原则（制度级）

基础履责决定绩效下限

T3+ 风险承担决定绩效上限

风险承担不能抵消失职

5.2 融合规则（强约束）
// 失职下拉
if (baseline_responsibility_status === 'fault') {
  R_final = Math.min(R_final, R2)
}

// 无 T3+ 封顶
if (!has_T3_plus_task) {
  R_final = Math.min(R_final, R3)
}

5.3 最终判定矩阵
baseline	是否 T3+	T3+性质	最终 R
good	否	–	R3
good	是	可免责失败 / 成果	R4
normal	否	–	R2
normal	是	可免责失败	R3
fault	任意	任意	R1 / R2
6. 数据存储建议（终评快照）
baseline_responsibility_status   TEXT
baseline_reasons                TEXT / JSON

incident_severity               TEXT
incident_count                  INTEGER
missed_deadline_count           INTEGER
unjustified_delay_count         INTEGER
process_violation_count         INTEGER
known_risk_unreported            BOOLEAN
repeated_issue_count             INTEGER


说明：

baseline_reasons 用于记录触发原因

所有字段必须可审计、可回放

7. UI 展示规范（简要）
7.1 终评页展示
基础履责状态： normal
触发原因：
- 同类问题跨周期重复
- 非关键任务多次延误

7.2 提示规则

fault 状态必须明确提示：

⚠️ 存在基础履责失职行为
该状态将下调最终绩效等级

8. 验收标准

无信号时，基础履责状态为 good

任意硬信号触发后，状态为 fault

轻微信号组合为 normal

fault 状态必然下调最终 R 等级

无 T3+ 任务时，R_final 不超过 R3

所有触发原因可在终评快照中回溯

9. 体系级说明（必须保留）

基础履责模块不是 KPI，
不是态度评价，
而是用于识别是否具备讨论绩效上限的资格。

失败可以免责，
失职不可以。