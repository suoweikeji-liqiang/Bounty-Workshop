# 开发计划与执行报告（迭代八）

## 1. 本轮目标

- 完成前端任务：
1. 按钮级权限控制
2. 执行闭环详情弹层与验收意见模板
- 补齐后端支撑接口

## 2. 后端交付

### 2.1 新增接口

- `GET /me`
- `GET /claims/mine`
- `GET /claims/{claim_id}/detail`
- `GET /deliverables/pending-acceptance/mine`

### 2.2 业务与模型输出

- 新增输出模型：
  - `ClaimExecutionRead`
  - `PendingAcceptanceRead`
  - `ClaimExecutionDetailRead`
  - `AcceptanceHistoryItem`
- 新增服务函数：
  - `get_my_profile`
  - `list_my_claims`
  - `list_my_pending_acceptance`
  - `get_claim_execution_detail`

### 2.3 回归

- `pytest -q` -> `6 passed`

## 3. 前端交付

### 3.1 按钮级权限控制

- 飞书页：
  - 同步按钮、频率保存仅 `admin`
  - reviewer/acceptor 保留查看能力
- 执行闭环页：
  - 验收提交仅 `admin/acceptor`
  - 激励确认仅 `admin/reviewer`
  - 成果提交按角色判断可见

### 3.2 执行闭环增强

- 新增 Claim 详情弹层（调用 `/claims/{id}/detail`）
  - 任务目标/范围/状态
  - 验收标准
  - 成果内容与达成项
  - 历史验收记录
- 新增验收意见模板机制
  - 结果切换（通过/整改/不通过）
  - 模板快捷填充
  - 支持自定义编辑后提交

### 3.3 前端构建验证

- `cd web && npm run build` -> 通过

## 4. 关键文件

- 后端：
  - `app/main.py`
  - `app/services.py`
  - `app/schemas.py`
- 前端：
  - `web/src/App.tsx`
  - `web/src/main.tsx`
  - `web/src/pages/ExecutionLoopPage.tsx`
  - `web/src/pages/FeishuPage.tsx`
  - `web/src/types.ts`
  - `web/src/index.css`

## 5. 下一步建议

1. 将验收模板配置化（系统配置页可维护）
2. 详情弹层补充附件预览与证据快速打开
3. 接入 Playwright 做前端端到端回归

---

# 开发计划与执行报告（迭代九）

## 1. 本轮目标

1. 落地“任务超期自动释放”后台定时作业  
2. 接入 Playwright 端到端回归主链路

## 2. 后端交付

### 2.1 定时作业能力

- 新增 `app/jobs.py`
  - 超期释放频率配置（默认 1440 分钟）
  - 后台循环作业：定时执行 `release_overdue_claims`
  - 支持环境变量开关：`ENABLE_BACKGROUND_JOBS`
- 应用生命周期接入作业启停（启动创建任务、关闭安全取消）

### 2.2 系统配置接口

- `GET /system/config/release-overdue-frequency`（admin）
- `PUT /system/config/release-overdue-frequency`（admin）

### 2.3 其他后端改进

- `app/db.py` 支持环境变量 `APP_DB_PATH`
- 增加 CORS 中间件，支持前端开发与 E2E 跨域访问

## 3. 前端交付

- 飞书集成页面新增“超期任务检查频率”配置区块（admin）
- 保留原飞书同步频率配置并并列展示

## 4. E2E 回归

### 4.1 Playwright 接入

- 新增配置：`web/playwright.config.ts`
- 新增脚本：`npm run e2e`
- 新增用例：`web/e2e/main-flow.spec.ts`
  - 覆盖流程：问题提交 -> 审核立项 -> 揭榜 -> 成果提交 -> 验收 -> 激励确认

### 4.2 运行结果

- `pytest -q` -> `9 passed`
- `cd web && npm run build` -> 通过
- `cd web && npm run e2e` -> `1 passed`
