# 积分与徽章体系改进方案

## 一、当前问题总结

| 问题 | 现状 | 影响 |
|------|------|------|
| 积分无规则 | 审核人自由填写任意数字 | 不同审核人标准不一，积分无公信力 |
| 徽章无体系 | 自由文本输入 | 徽章命名混乱，无法形成统一的荣誉认知 |
| 团队积分不分割 | 每个成员拿全额积分 | 与金额按比例分配的逻辑不一致，规则不透明 |
| 提交人无积分 | proposer 的 reward 记录 points=0 | 对问题发现者缺乏积分激励 |
| 积分无用途 | 仅展示和排行 | 积分缺乏实际价值感，激励效果弱 |
| 徽章无元数据 | 纯字符串存储和展示 | 无图标、无等级、无描述，视觉吸引力差 |

## 二、积分体系改进

### 2.1 建立等级-积分对照表

与现有的 `LEVEL_REWARD_RANGE` 对齐，新增积分范围配置：

```
LEVEL_POINTS_RANGE = {
    S: (80, 150),
    A: (40, 80),
    B: (15, 40),
    C: (5, 15),
}
```

审核人定价时，积分输入框增加范围提示和校验，随任务等级联动。

### 2.2 积分分配规则

| 角色 | 分配方式 | 说明 |
|------|----------|------|
| 提交人 (proposer) | 获得任务积分 × proposer_ratio | 发现问题也应获得积分激励 |
| 执行人 (executor) 单人 | 获得任务积分 × (1 - proposer_ratio) | 与金额分配逻辑一致 |
| 执行人 (executor) 团队 | 按 member.ratio 比例分配执行人积分 | 与金额分配逻辑一致 |

示例：S 级任务 100 积分，proposer_ratio=0.3
- 提交人获得 30 积分
- 执行人共获得 70 积分（团队按比例再分）

### 2.3 积分校验

后端 `PricingDefinition` 和 `TaskDefinition` 增加积分范围校验，类似现有的 `validate_reward_range`：

```python
# 伪代码示意
low, high = LEVEL_POINTS_RANGE[self.level]
if not (low <= self.points <= high):
    raise ValueError(f"{self.level} 等级积分范围应在 {low}-{high}")
```

## 三、徽章体系改进

### 3.1 预定义徽章库

新建 `Badge` 模型或配置表，预定义系统徽章：

| 徽章 ID | 名称 | 类别 | 获取条件 | 图标建议 |
|---------|------|------|----------|----------|
| `innovator` | 创新先锋 | 任务类 | 完成 S 级任务 | 灯泡 |
| `problem_hunter` | 问题猎手 | 提交类 | 累计提交 10 个通过审核的问题 | 放大镜 |
| `speed_runner` | 效率达人 | 时效类 | 提前完成 3 个任务（未超期） | 闪电 |
| `team_player` | 协作之星 | 团队类 | 参与 5 次团队揭榜 | 握手 |
| `quality_king` | 品质标杆 | 质量类 | 连续 5 次验收一次通过 | 皇冠 |
| `first_blood` | 首次揭榜 | 里程碑 | 第一次完成任务 | 旗帜 |
| `centurion` | 百分达人 | 里程碑 | 累计积分达到 100 | 奖杯 |
| `veteran` | 资深贡献者 | 里程碑 | 累计完成 20 个任务 | 勋章 |

### 3.2 徽章获取方式改为双轨制

**手动授予**（保留现有能力，但改为下拉选择）：
- 审核人定价时从预定义列表中选择徽章，而非自由输入
- 可多选，一个任务可授予多个徽章

**自动授予**（新增）：
- 系统在任务完成、奖励确认等节点自动检查用户是否满足徽章条件
- 满足条件时自动发放，并在个人中心显示通知

### 3.3 数据模型调整

**Badge 表（新增）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int, PK | 主键 |
| code | str, unique | 唯一标识 (如 "innovator") |
| name | str | 显示名称 |
| category | str | 类别 (task/submit/speed/team/quality/milestone) |
| description | str | 获取条件描述 |
| icon | str | 图标标识 |
| auto_rule_json | str, optional | 自动授予规则 (JSON) |
| sort_order | int | 排序权重 |
| enabled | bool | 是否启用 |

**UserBadge 表（新增，替代 Reward.badge）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int, PK | 主键 |
| user_id | int, FK | 用户 |
| badge_id | int, FK | 徽章 |
| source_type | str | 来源 ("task_reward" / "auto_achievement") |
| source_id | int, optional | 来源 ID (task_id 或 null) |
| earned_at | datetime | 获得时间 |

约束：`unique(user_id, badge_id)` — 同一徽章不重复授予。

Reward 表的 `badge` 字段改为 `badge_id`（外键关联 Badge 表），或保留为记录快照。

## 四、积分应用场景（可选扩展）

为积分赋予实际价值，建议分阶段实施：

**第一阶段 — 荣誉展示（当前已有，优化）**
- 个人中心积分卡片 + 排行榜（已有）
- 增加积分变动历史记录

**第二阶段 — 权益解锁**
- 积分达到阈值可解锁特殊权限，如：
  - 100 分：可优先揭榜 S 级任务
  - 200 分：可推荐知识库条目
  - 500 分：获得"年度贡献者"称号

**第三阶段 — 积分消费（远期）**
- 积分兑换实物/虚拟奖品
- 积分抵扣（如用积分换取额外假期等，需与 HR 系统对接）

## 五、前端改动要点

| 页面 | 改动 |
|------|------|
| ReviewWorkbenchPage | 积分输入框增加范围提示，随等级联动；徽章改为下拉多选 |
| BudgetReviewPage | 展示积分范围是否合规 |
| PersonalCenterPage | 徽章展示增加图标和描述 tooltip；增加积分变动明细 |
| DashboardPage | 积分排行保持不变；可增加徽章排行（按徽章数量） |

## 六、实施优先级建议

| 优先级 | 改动项 | 工作量 | 价值 |
|--------|--------|--------|------|
| P0 | 积分等级范围校验 | 小 | 消除积分随意性 |
| P0 | 积分按比例分配（与金额一致） | 小 | 修复逻辑不一致 |
| P1 | 预定义徽章库 + 下拉选择 | 中 | 建立统一徽章体系 |
| P1 | 提交人也获得积分 | 小 | 完善激励覆盖 |
| P2 | 自动徽章授予 | 中 | 增加趣味性和参与感 |
| P2 | 徽章图标和元数据展示 | 中 | 提升视觉体验 |
| P3 | 积分应用场景 | 大 | 赋予积分实际价值 |
