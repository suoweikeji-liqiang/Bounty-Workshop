# 揭榜挂帅工坊（Bounty Workshop）代码审查报告

---

## 1. 审查范围 & 方法

| 层级 | 文件 |
|---|---|
| **后端** | `models.py`, `enums.py`, `auth.py`, `schemas.py`, `main.py`, `services.py`, `jobs.py`, `attachments.py`, `reporting.py` |
| **前端** | `App.tsx`, `Bootstrap.tsx`, `types.ts`, `http.ts`, 15 个页面组件 |
| **需求文档** | `软件需求规格说明书.md`, `补充需求.md` |

审查维度：**功能完整性、交互性、权限控制、安全性、代码质量**。

---

## 2. 问题总览

| 优先级 | 数量 | 分类 |
|---|---|---|
| 🔴 **严重 (Critical)** | 4 | 认证绕过、权限缺失、数据篡改 |
| 🟠 **高 (High)** | 6 | 业务逻辑漏洞、安全隐患 |
| 🟡 **中 (Medium)** | 8 | 功能缺陷、健壮性不足 |
| 🔵 **低 (Low)** | 5 | 代码质量、可维护性 |

---

## 3. 🔴 严重问题 (Critical)

### C-1: 登录接口无密码认证

> **⚠️ CAUTION**
>
> 登录 API `/auth/login` 仅凭 `user_id` 或 `employee_no` 即可获取 token，**无需密码**。

**文件**: `auth.py` / `main.py`

```python
# auth.py - login_by_user_id
def login_by_user_id(session: Session, user_id: int) -> dict:
    user = session.get(User, user_id)
    if user is None:
        raise HTTPException(status_code=404, ...)
    # 直接签发 token，无密码校验
    token = create_access_token(user.id)
    ...
```

- 前端 `LoginPage.tsx` 直接填入 user_id 即可登录
- 攻击者可遍历 ID 获取任意用户的 token
- **建议**: 至少在非飞书登录模式下要求密码或限制为开发环境

---

### C-2: X-User-Id 请求头残留安全隐患

> **⚠️ CAUTION**
>
> 前端 `http.ts` 仍然在每个请求中附带 `X-User-Id` 头。

**文件**: `http.ts`

```typescript
function buildHeaders(options: HttpOptions): Record<string, string> {
  // ...
  if (typeof options.userId === 'number' && Number.isFinite(options.userId)) {
    headers['X-User-Id'] = String(options.userId)
  }
  return headers
}
```

虽然后端已切换到 Bearer Token 认证，但 `X-User-Id` 头作为遗留逻辑仍被发送。若后端有任何中间件或日志依据该头做决策，**可被伪造用户身份**。

---

### C-3: 多个 API 端点缺少权限校验

> **⚠️ CAUTION**
>
> 以下重要端点没有 `require_roles` 依赖，任何已认证用户均可调用。

**文件**: `main.py`

| 端点 | 风险 |
|---|---|
| `POST /tasks/{id}/claims` | 任意用户可揭榜任何任务 |
| `POST /claims/{id}/deliverables` | 任意用户可向别人的揭榜提交成果 |
| `POST /deliverables/{id}/accept` | 后端 services 层做了检查，但 route 层无 Guard |
| `GET /users/active`, `/users/acceptors` | 可泄露所有活跃用户信息 |
| `POST /system/trigger-...` | 后端有角色检查但路由层未声明 |

**影响**: 即使 service 层做了部分检查，缺少路由层统一 Guard 会导致防御不一致。

---

### C-4: 附件下载无权限校验

> **⚠️ CAUTION**
>
> `/attachments/{id}/download` 端点任何已登录用户可下载任意附件。

**文件**: `main.py` + `attachments.py`

```python
@app.get("/attachments/{attachment_id}/download")
async def download_attachment(attachment_id: int, session=..., _user_id=...):
    att = read_attachment(session, attachment_id)
    # 无任何 entity 归属校验
```

- 未检查附件的 `entity_type`/`entity_id` 归属关系
- 攻击者可遍历 attachment_id 下载其他用户的附件
- **建议**: 校验当前用户是否有权访问关联实体

---

## 4. 🟠 高优先级问题 (High)

### H-1: 任务状态机不完整

**文件**: `services.py`

- `accept_deliverable` 中 `REJECTED` 直接将 claim 设为 `ABANDONED`，task 设为 `OPEN`，但**未检查同一任务是否还有其他活跃 claim**
- `abandon_claim` 正确检查了活跃 claim 数，但 `accept (rejected)` 未做同样检查
- 可能导致有其他活跃执行者的任务被错误重置为 OPEN

### H-2: 团队揭榜比例验证不严格

**文件**: `services.py`

- Schema 层 `TeamClaimMembers` 验证了成员比例和之和
- 但 `claim_task` 函数中用 `if total_ratio > 1.001` 做浮点比较，精度阈值与 Schema 不一致（Schema 用 `abs(total - 1.0) > 1e-6`）
- **建议**: 统一浮点精度容差

### H-3: 奖励分配浮点精度问题

**文件**: `services.py`

```python
proposer_amount = round(task.reward_total * task.proposer_ratio, 2)
executors_amount = round(task.reward_total - proposer_amount, 2)
for member in members:
    amount = round(executors_amount * member.ratio, 2)
```

- 多次 `round` 导致**总分配金额可能不等于 `reward_total`**
- 特别是团队 3 人以上时，各 `round(amount, 2)` 之和可能与 `executors_amount` 有差异
- **建议**: 使用"最后一人补差"或 `Decimal` 精确计算

### H-4: 超期释放逻辑的边界条件

**文件**: `services.py`

```python
if task.due_date >= today:
    continue  # 未过期
```

- `due_date >= today` 意味着当天到期的不释放，**次日才释放**
- 但需求中"到期日"语义需明确：是"含当天"还是"不含当天"
- 此外 `due_date` 是 `date` 类型但 schema 接收 `str`，缺少时区处理

### H-5: `release_overdue_claims` 全表扫描

**文件**: `services.py`

```python
claims = session.exec(select(Claim).where(Claim.status == ClaimStatus.ACTIVE)).all()
```

- 查出所有 ACTIVE claims 后逐个检查 task 的 due_date
- 数据量大时性能差，应在数据库层 JOIN 筛选

### H-6: 前端路由 Guard 与导航栏不一致

**文件**: `App.tsx`

| 页面 | 导航栏可见性 | 路由 Guard | 差异 |
|---|---|---|---|
| `/claim-approvals` | 所有用户可见 | **无 Guard** | ❌ 缺少保护 |
| `/execution` | 所有用户可见 | **无 Guard** | ❌ 缺少保护 |
| `/tasks` | 所有用户可见 | **无 Guard** | ⚠️ 内含管理操作 |
| `/attachments` | 所有用户可见 | **无 Guard** | ⚠️ 可上传下载 |

---

## 5. 🟡 中优先级问题 (Medium)

### M-1: `datetime.utcnow()` 已弃用

**全局问题**（`models.py`, `services.py`, `auth.py`）

`datetime.utcnow()` 在 Python 3.12+ 已弃用，应使用 `datetime.now(timezone.utc)`。

### M-2: Token 过期时间无刷新机制

**文件**: `auth.py`

- Token 默认 480 分钟（8 小时）后过期
- 无 refresh token 机制
- 活跃用户在使用过程中可能突然过期被踢出

### M-3: Problem → Task 审核缺少并发控制

**文件**: `services.py`

- `review_problem` 在检查 `problem.status == ProblemStatus.PENDING_REVIEW` 后修改状态
- 两个 reviewer 同时审核同一问题可能导致重复立项
- **建议**: 使用乐观锁或 `SELECT ... FOR UPDATE`

### M-4: 知识库搜索 SQL 注入风险

**文件**: `services.py`

```python
like_value = f"%{needle}%"
statement = statement.where(
    func.lower(Knowledge.problem_summary).like(like_value),
)
```

- 虽然 SQLModel/SQLAlchemy 的 `.like()` 会参数化，但 `%` 和 `_` 是 LIKE 的通配符
- 用户输入的 `%` 或 `_` 不会被转义，可能导致意外匹配
- **建议**: 转义 LIKE 特殊字符

### M-5: 前端 localStorage 存储 claim 映射不可靠

**文件**: `TaskHallPage.tsx`

```typescript
function saveClaimRecord(taskId: number, claimId: number) {
  const map = raw ? (JSON.parse(raw) as ...) : {}
  map[String(taskId)] = claimId
  localStorage.setItem(claimStorageKey, JSON.stringify(map))
}
```

- 使跨设备/跨浏览器时数据不同步
- 清除浏览器数据后映射丢失
- 应在后端提供 "我的 claims 对应的 task" 查询

### M-6: 执行闭环页面提交的 `criteria_results` 硬编码

**文件**: `ExecutionLoopPage.tsx`

```typescript
body: {
  summary,
  criteria_results: ['来自执行闭环页面提交'],  // 硬编码
  evidence_attachment_ids: evidenceAttachmentIds,
}
```

与 `TaskHallPage.tsx` 的提交成果表单不同（后者有完整的验收项输入），执行闭环页面**硬编码了一条固定文本**，丧失了验收项逐项描述的能力。

### M-7: `_to_json` / `_from_json` 分散在 services 中

Schema 层使用 Pydantic 验证，但数据库存储为JSON字符串，手动序列化/反序列化分散且缺少统一错误处理。

### M-8: 报表导出无权限限制

**文件**: `main.py`

`GET /export/tasks`, `/export/rewards`, `/export/knowledge` 等端点仅要求登录，**任何角色均可导出全量数据**（含奖励金额、用户信息等敏感数据）。

---

## 6. 🔵 低优先级问题 (Low)

### L-1: 重复的 `hasAnyRole` 函数

`hasAnyRole` 在 `App.tsx`、`TaskHallPage.tsx`、`ClaimApprovalPage.tsx` 中重复定义，应提取为公共 util。

### L-2: Toast 消息模式重复

每个页面组件都有相同的 `useEffect(() => { if (!error) return; toast.error(error) }, [error, toast])` 模式，应封装为自定义 Hook。

### L-3: 缺少分页机制

`/problems`、`/tasks`、`/rewards` 等列表 API 返回全量数据，大数据量时前端性能差。`list_knowledge` 有分页但其他接口没有。

### L-4: 缺少单元测试

未发现 `tests/` 目录或任何测试文件，关键业务逻辑（绩效计算、奖励分配、状态机流转）缺少自动化验证。

### L-5: 环境配置缺少文档

`AUTH_TOKEN_SECRET`、`ATTACHMENT_S3_BUCKET` 等环境变量缺少 `.env.example` 或配置文档。

---

## 7. 需求符合度评估

### 功能矩阵

| 模块 | 需求 | 实现状态 | 备注 |
|---|---|---|---|
| 用户管理 | Feishu 同步 + 本地管理 | ✅ 完整 | |
| 问题提报 | 多维度描述 + 附件 | ✅ 完整 | |
| 审核立项 | 审批/驳回/合并 + 任务定义 | ✅ 完整 | |
| 揭榜机制 | 个人/团队 + 超期审批 | ✅ 完整 | 比例精度问题 (H-3) |
| 成果验收 | 三结果 + 模板 + 历史 | ✅ 完整 | |
| 绩效评估 | 基础履责 + R级融合 | ✅ 完整 | 与补充需求高度一致 |
| 激励发放 | 绩效挂钩 + Hold 策略 | ✅ 完整 | |
| 知识库 | 自动归档 + 搜索 | ✅ 完整 | LIKE 注入 (M-4) |
| 数据看板 | 概览/排行/趋势/分布 | ✅ 完整 | |
| 数据导出 | Excel/PDF | ✅ 完整 | 权限缺失 (M-8) |

### 权限矩阵对照

| 角色 | 需求权限 | 实现情况 |
|---|---|---|
| `employee` | 提报问题、揭榜、提交成果 | ⚠️ 端点无 Guard (C-3) |
| `reviewer` | 审核问题、定义任务、激励复核 | ✅ 路由 + 服务层 |
| `acceptor` | 验收成果 | ✅ 服务层检查 `accepter_id` |
| `admin` | 全部权限 + 系统配置 | ✅ Guard 覆盖 |

---

## 8. 优先修复建议

1. **立即修复**: C-1 ~ C-4（安全漏洞）
2. **短期修复**: H-1 ~ H-6（业务逻辑 + 前端 Guard）
3. **中期改进**: M-1 ~ M-8（健壮性 + 代码质量）
4. **长期优化**: L-1 ~ L-5（可维护性 + 测试覆盖）

---

## 9. 总结

系统**功能覆盖度高**，需求规格说明书中的核心业务流（问题 → 审核 → 任务 → 揭榜 → 执行 → 验收 → 激励 → 知识库）均有完整实现。补充需求中的绩效评估模块（基础履责判定 + R级融合规则）实现严格对应文档定义。

**主要风险集中在安全层面**：登录无密码、路由层权限 Guard 不完整、附件下载无归属校验。这些问题在内部系统中可控，但若暴露公网则为严重漏洞。建议优先解决 4 个 Critical 级问题。
