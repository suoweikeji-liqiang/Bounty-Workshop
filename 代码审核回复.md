# 代码审核回复

回复时间：2026-02-25
回复人：开发团队
审查来源：Gemini 审查报告、Codex 审查报告

---

## 一、本轮已修复项

以下问题在本次自测中已完成修复并通过全部 20 个测试用例：

| # | 问题 | 来源 | 修复说明 |
|---|------|------|----------|
| 1 | SQLAlchemy WHERE 子句中 `is True` 不生效 | 自测发现 | `ai_models.py` 全部改为 `== True` |
| 2 | `_trigger_analysis_background` 中 `get_session` 上下文管理器 TypeError | 自测发现 | 改为 `Session(engine)` 直接构造 |
| 3 | `/problems` 端点缺少 `offset`/`limit` 参数透传 | 自测发现 | 补齐参数并透传至 `list_problems` |
| 4 | `/tasks` 端点缺少 `offset`/`limit` 参数透传 | 自测发现 | 补齐参数并透传至 `list_tasks` |
| 5 | `/rewards` 端点缺少 `offset`/`limit` 参数透传 | 自测发现 | 补齐参数并透传至 `list_rewards` |
| 6 | 附件下载 `/attachments/{id}/download` 缺少实体级权限校验 | Codex P0-2 | 增加 `ensure_entity_attachment_access` 调用 |
| 7 | 实体附件列表 `/entities/{type}/{id}/attachments` 缺少权限校验 | Codex P0-2 | 同上，增加实体级 ACL |
| 8 | Anthropic API 适配（system 消息分离、header） | 自测发现 | `prodmind.py` 重构为独立调用 + 正确的 API 格式 |
| 9 | AI 返回非法枚举值导致整条分析失败 | 自测发现 | 增加枚举 fallback |
| 10 | 团队揭榜比例之和未校验 | 自测发现 | `services.py` 增加 ratio 求和校验 |
| 11 | 验收通过后同任务其他活跃揭榜悬挂 | 自测发现 | 验收时自动将其他活跃 claim 置为 ABANDONED |
| 12 | 激励重复确认无幂等保护 | 自测发现 | 增加已确认状态检查 |
| 13 | `except (ValueError, Exception)` 冗余捕获 | Codex P2-2 | 简化为 `except Exception` |

---

## 二、逐项回复：严重问题（P0 / Blockers）

### 2.1 硬编码初始密码与 JWT 密钥（Gemini Blocker-1 / Codex P0-3）

**状态：认可，计划修复**

两份报告均指出此问题，确认存在：
- `lifespan` 中超级管理员默认密码 `admin123` 硬编码且打印到日志
- `auth.py` 中 JWT 密钥回退值 `bounty-workshop-dev-secret`
- 非 production 环境默认开启免密登录和 `X-User-Id` 头鉴权

修复计划：
1. 初始管理员密码改为从环境变量 `INIT_ADMIN_PASSWORD` 读取，缺失时生成随机密码并仅首次打印
2. `AUTH_TOKEN_SECRET` 缺失时拒绝启动（生产环境）
3. `AUTH_ENABLE_PASSWORDLESS_LOGIN` 和 `AUTH_ENABLE_HEADER_USER_ID` 默认值改为 `False`，仅开发环境通过 `.env` 显式开启

### 2.2 附件上传内存溢出风险（Gemini Blocker-2）

**状态：认可，计划修复**

当前 `await file.read()` 一次性读入全部内容，大文件场景确实存在 OOM 风险。

修复计划：
1. 增加上传文件大小上限校验（如 50MB），超限直接 413 拒绝
2. 本地存储改为 `shutil.copyfileobj(file.file, buffer)` 流式写入
3. S3 存储改为 `upload_fileobj` 直传文件流，避免内存中转

### 2.3 SQLite 并发配置缺失（Gemini Blocker-3）

**状态：认可，计划修复**

当前 `db.py` 仅设置了 `check_same_thread=False`，缺少 WAL 模式和超时配置。

修复计划：
1. `connect_args` 增加 `timeout=15`
2. 通过 `event.listens_for(engine, "connect")` 开启 `PRAGMA journal_mode=WAL` 和 `PRAGMA synchronous=NORMAL`

### 2.4 系统配置接口运行时崩溃（Codex P0-1）

**状态：认可，计划修复**

`feishu.py` 中三处 `model_dump_json(ensure_ascii=False)` 在当前 Pydantic 版本触发 `TypeError`，导致验收模板相关接口 500。

修复计划：
1. 统一改为 `json.dumps(model.model_dump(), ensure_ascii=False)`
2. 补充对应接口的回归测试用例

### 2.5 附件权限控制链路不完整（Codex P0-2）

**状态：部分已修复，剩余计划修复**

已修复：
- `/attachments/{id}/download` 已增加实体级 ACL
- `/entities/{type}/{id}/attachments` 已增加实体级 ACL

待修复：
- `/attachments/{id}` 元数据接口缺少实体级校验
- `/attachments/{id}/presign` 预签名接口缺少实体级校验
- 未绑定实体的附件下载缺少 uploader/admin 约束
- 绑定附件时未校验上传者归属，存在"复绑"风险

修复计划：
1. 元数据和预签名接口复用 `ensure_entity_attachment_access`
2. 未绑定实体的附件仅允许上传者本人和管理员访问
3. 绑定接口增加上传者一致性校验和"已绑定不可复绑"约束

### 2.6 AI 密钥加密键硬编码并写入 compose（Codex P0-4）

**状态：认可，计划修复**

当前 `ai_models.py` 中固定兜底密钥 + `docker-compose.yml` 明文写入同一密钥，数据库泄露后可批量还原 API Key。

修复计划：
1. 移除 `_DEFAULT_KEY` 兜底值，启动时若 `AI_ENCRYPTION_KEY` 缺失则拒绝启动
2. `docker-compose.yml` 中改为引用 `.env` 文件，不在版本控制中提交密钥明文
3. 文档补充密钥轮换说明

---

## 三、逐项回复：重要问题（P1）

### 3.1 分析能力的对象级权限缺失（Codex P1-1）

**状态：认可，计划修复**

当前分析接口仅做角色校验（ADMIN/REVIEWER/EMPLOYEE），任意员工可按 `problem_id` 访问不属于自己的分析内容。

修复计划：
1. 复用 `get_problem_detail` 的对象级权限规则（提交人/审核人/管理员）
2. 触发分析接口同样增加归属校验，防止 AI 调用成本被滥用

### 3.2 评审引用与分析记录缺少一致性校验（Codex P1-2）

**状态：认可，计划修复**

`services.py` 中评审通过时仅校验 `analysis_id` 存在，未校验 `analysis.problem_id == problem_id`，可能将 A 问题的分析错误挂到 B 问题。

修复计划：
1. `review_problem` 中增加 `analysis.problem_id == problem_id` 一致性检查
2. 手工创建 analysis-ref 时同样增加归属校验

### 3.3 前端附件下载与鉴权机制不兼容（Codex P1-3）

**状态：认可，计划修复**

前端使用 `<a href>` 直连下载，不会携带 `Authorization` 头，Bearer 鉴权场景下会 401。

修复计划：
1. 改为 `fetch` + Blob 下载方式，请求时携带 token
2. 或统一走 `/attachments/{id}/presign` 获取一次性签名 URL（需先修复该接口的 ACL）

### 3.4 docker-compose 默认公开 MinIO 桶（Codex P1-4）

**状态：认可，计划修复**

`mc policy set public` 使桶内对象可匿名访问，与最小权限原则冲突。

修复计划：
1. 改为 `private` 策略，所有下载走后端 presign 接口
2. 生产环境文档中说明公开资源桶与业务证据桶的分离方案

---

## 四、逐项回复：一般问题（P2 / Suggestions）

### 4.1 密码重置接口请求体与路径参数重复（Codex P2-1）

**状态：认可，计划修复**

`SetPasswordRequest` 中包含 `user_id` 字段，但路由已通过路径参数 `{user_id}` 获取，body 中的字段被忽略，API 契约混乱。

修复计划：从 `SetPasswordRequest` schema 中移除 `user_id`，仅保留 `new_password` 和 `force_change`。

### 4.2 后台分析任务异常被吞掉（Codex P2-2）

**状态：部分已修复**

已将 `except (ValueError, Exception)` 简化为 `except Exception`，但仍为静默吞没。

修复计划：增加 `logging.exception()` 结构化日志输出，便于线上排障。

### 4.3 巨型单体文件拆分（Gemini Suggestion-1）

**状态：认可，纳入技术债**

`main.py`（~1500行）和 `services.py`（~1600行）确实过于臃肿。

计划：后续迭代中使用 FastAPI `APIRouter` 按业务域拆分（users、problems、tasks、claims、deliverables、rewards、knowledge），`services.py` 同步按域拆分。当前阶段优先修复功能和安全问题，架构重构排入下一迭代。

### 4.4 前端巨型 CSS 文件（Gemini Suggestion-2）

**状态：认可，纳入技术债**

`web/src/index.css` 达 24KB，缺少局部作用域。后续重构时考虑引入 CSS Modules 或 Tailwind CSS 进行组件化样式隔离。优先级较低。

### 4.5 HTTP 中间件异常处理（Gemini Suggestion-3）

**状态：认可，计划修复**

当前未捕获异常直接返回无结构的 500 响应。

修复计划：配置 FastAPI 全局 `Exception Handler`，统一返回 `{"detail": "Internal Server Error"}` 格式。

---

## 五、Gemini 报告亮点确认

感谢 Gemini 报告对以下方面的肯定：
- RBAC 纵深防御（后端 `require_roles` + 前端 `<Guard>` 组件）
- 全面使用 ORM 杜绝 SQL 注入
- Type Hint + Pydantic 深度结合保障数据契约

这些设计原则将在后续迭代中继续坚持。

---

## 六、修复优先级与排期

| 优先级 | 问题项 | 状态 |
|--------|--------|------|
| **已完成** | 附件下载/列表 ACL、分页透传、SQLAlchemy 查询修复、Anthropic 适配、枚举 fallback、揭榜比例校验、悬挂揭榜清理、激励幂等、冗余异常捕获 | ✅ 已修复 |
| **P0 第一批** | 系统配置接口崩溃（feishu.py model_dump_json）、初始密码/JWT 密钥硬编码、AI 加密键硬编码 | 🔴 计划本周内修复 |
| **P0 第二批** | 附件上传 OOM、SQLite WAL 配置、附件元数据/presign ACL、附件绑定归属校验 | 🔴 计划本周内修复 |
| **P1** | 分析对象级权限、评审引用一致性校验、前端下载鉴权、MinIO 桶策略 | 🟡 计划下周修复 |
| **P2/技术债** | Schema 契约清理、后台异常日志、全局异常 Handler、文件拆分、CSS 优化 | 🟢 纳入迭代计划 |

