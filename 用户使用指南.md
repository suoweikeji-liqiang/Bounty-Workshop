# Bounty Workshop 用户使用指南

本文档面向业务用户与管理员，介绍系统登录、核心流程、常见操作与问题处理。

## 1. 系统目标

Bounty Workshop 用于把“问题改进”转成可跟踪的任务闭环：

- 提交问题
- 审核立项
- 揭榜执行
- 提交成果
- 验收结论
- 激励确认
- 知识沉淀

## 2. 角色说明

- `employee`：提交问题、参与/执行任务、提交成果、查看个人中心。
- `reviewer`：审核问题、查看操作日志、处理超期揭榜审批、确认激励。
- `acceptor`：对成果做验收（通过/返工/驳回）。
- `admin`：拥有全部权限，并可管理系统配置、用户、飞书集成等。

说明：一个账号可以同时拥有多个角色。

## 3. 登录与退出

登录页支持两种方式：

- `User ID`（开发/测试环境常用）
- `Employee No`（工号）

登录成功后系统会自动保存会话 Token。点击右上角 `logout` 可退出。

## 4. 功能导航

左侧菜单通常包含：

- `Dashboard`：全局看板（问题、任务、奖励、趋势、分布）
- `Personal`：个人中心（积分、奖励、徽章、个人统计）
- `Problems`：问题提报与我的问题
- `Review`：问题审核与立项（reviewer/admin）
- `Task Hall`：任务大厅（揭榜、团队揭榜、提交成果）
- `Claim Approvals`：超期揭榜审批
- `Execution`：执行闭环（验收、奖励确认）
- `Knowledge`：知识库（检索、分页、详情）
- `Attachments`：附件上传与查询
- `Operation Logs`：操作日志（reviewer/admin）
- `System Config`：系统配置（admin）
- `Users`：用户与角色管理（admin）
- `Feishu`：飞书 OAuth 与同步（admin/reviewer/acceptor）

## 5. 业务主流程

### 5.1 提交问题（Problems）

1. 填写标题、场景、背景、问题描述、价值说明等。
2. 可填写附件 ID（先在 `Attachments` 上传后得到）。
3. 点击提交，状态进入待审核。

### 5.2 审核立项（Review）

由 `reviewer/admin` 在审核页处理：

- 驳回：填写驳回原因，可选合并到已有问题。
- 立项：填写任务目标、范围、截止日期、奖励、验收人、验收标准。

立项后自动生成任务进入任务大厅。

### 5.3 揭榜执行（Task Hall）

可对 `open` 或 `in_progress` 任务揭榜：

- 个人揭榜：单人负责。
- 团队揭榜：设置主负责人和成员占比，占比总和必须为 1。

注意：当用户超期次数达到阈值时，可能无法自行揭榜，需要审批流程。

### 5.4 提交成果（Task Hall / Execution）

执行人提交：

- 成果摘要
- 验收结果说明（按任务验收标准动态填写）
- 证据附件 ID

提交后任务进入待验收。

### 5.5 验收（Execution）

由 `acceptor/admin` 对成果做结论：

- `approved`：通过
- `rework`：返工
- `rejected`：驳回

可使用系统内置评论模板快速填写意见。

### 5.6 激励确认（Execution）

由 `reviewer/admin` 确认奖励记录，确认后计入个人奖励与积分。

### 5.7 知识沉淀（Knowledge）

验收通过后，系统会自动沉淀为知识条目。支持：

- 关键词搜索
- 场景/等级筛选
- 推荐筛选
- 分页浏览

## 6. 附件使用说明

在 `Attachments` 页面可：

- 上传文件获取附件 ID
- 按附件 ID 查询详情
- 按实体（problem/deliverable）查询关联附件

上传后可在问题提交、成果提交时引用附件 ID。

## 7. 超期揭榜审批说明

当系统判断用户超期风险达到阈值时：

- 用户直接揭榜会被拦截
- 系统会创建审批请求
- `reviewer/admin` 可在 `Claim Approvals` 审批同意/拒绝

审批通过后可继续任务流程。

## 8. 系统配置（管理员）

`System Config` 可统一管理：

- 飞书同步频率
- 超期释放任务频率
- 超期揭榜阈值
- 验收评论模板

修改后即时生效。

## 9. 常见提示与处理

- `401`：未登录或会话过期，重新登录。
- `403`：权限不足，联系管理员分配角色。
- `429`：触发限流，稍后重试（揭榜/成果提交接口有保护）。
- “请先选择任务/claim_id”等提示：按页面要求补全必填项。
- 团队占比错误：确保成员至少两人且比例总和为 1。

## 10. 最佳实践建议

- 问题标题尽量具体，便于后续检索和复盘。
- 验收标准尽量可量化，减少主观争议。
- 成果提交尽量附证据（附件或链接），提高验收效率。
- 定期在 `Knowledge` 复用历史方案，减少重复劳动。

